{% extends 'base.html.twig' %}

{% block title %}Choisir la méthode 2FA{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">

            <h2 class="text-2xl font-bold mb-6 text-center">Authentification à deux facteurs</h2>
            <p class="text-gray-600 mb-6 text-center">Veuillez choisir votre méthode d'authentification à deux facteurs préférée</p>

            {% if error is defined %}
                {{ component('Alert', {message: error, color: 'bg-red-400', icon: 'warning'}) }}
            {% endif %}

            <form method="post">
                <div class="flex flex-col gap-4 mb-6">
                    {% for id, provider in providersData %}
                        <div>
                            <input type="radio" id="{{ id }}" name="auth_method" value="{{ id }}" class="sr-only peer">
                            <label for="{{ id }}" class="flex p-5 bg-gray-50 border border-gray-300 rounded-lg cursor-pointer focus:outline-none hover:bg-gray-100 peer-checked:ring-2 peer-checked:ring-blue-500 peer-checked:border-transparent">
                                <div class="flex items-center">
                                    <div class="w-10 h-10 flex items-center justify-center rounded-full bg-blue-100 text-blue-500 mr-4">
                                        {{ ux_icon(provider.icon, {class: 'h6 w-6'}) }}
                                    </div>
                                    <div>
                                        <span class="block text-sm font-bold text-gray-800">{{ provider.title }}</span>
                                        <span class="block text-xs text-gray-600">{{ provider.description }}</span>
                                    </div>
                                </div>
                            </label>
                        </div>
                    {% endfor %}
                </div>


                <div class="flex gap-4 justify-between">
                    <a href="#"
                       class="w-1/2 py-2 px-4 border border-gray-300 rounded-lg text-gray-700 text-center hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-300"
                    >
                        Annuler
                    </a>
                    <button type="submit"
                            class="w-1/2 bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
                    >
                        Continuer
                    </button>
                </div>
            </form>
        </div>
    </div>
{% endblock %}